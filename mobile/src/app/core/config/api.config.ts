export const API_CONFIG = {
  // Browser: Laravel running on the same development machine.
  // Physical Android/iOS device: replace with the PC's LAN address, e.g.
  // http://192.168.1.10:8000/api
  baseUrl: 'http://localhost:8000/api',
};
